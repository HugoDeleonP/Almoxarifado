version: '3.8'
services:
  db:
    image: mysql/mysql-server:latest
    container_name: almoxarifadoIndustrial
    restart: always
    environment:
      - MYSQL_DATABASE=almoxarifadoIndustrial
      - MYSQL_ROOT_PASSWORD=1234
    ports:
      - '3308:3306'
    volumes:
      - mysql-volume:/var/lib/mysql
volumes:
  mysql-volume:
    driver: local